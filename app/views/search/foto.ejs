<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container mt-5 text-center">
    <h2 class="mb-4">Detalle de Foto - ID <%= foto.id %></h2>

    <div class="row justify-content-center">
      <!-- Fila 1: Información -->
      <div class="col-md-6 mb-4 text-start">
        <p><strong>Título:</strong> <%= foto.titulo %></p>
        <p><strong>Descripción:</strong> <%= foto.descripcion %></p>
        <p><strong>Calificación:</strong>
          <span class="badge rounded-pill px-3 py-2 bg-info text-dark">
            <%= foto.calificacion %>
          </span>
        </p>
        <p><strong>Etiquetas:</strong><br>
          <% if (foto.Etiqueta && foto.Etiqueta.length > 0) { %>
            <% foto.Etiqueta.forEach(etiqueta => { %>
              <span class="badge bg-secondary me-1 mb-1"><%= etiqueta.texto %></span>
            <% }); %>
          <% } else { %>
            <span class="text-muted">Sin etiquetas</span>
          <% } %>
        </p>
      </div>

      <!-- Fila 2: Imagen -->
      <div class="col-md-6 mb-4">
        <img src="/<%= foto.ruta %>" alt="Foto" class="img-fluid rounded shadow">
      </div>
    </div>

    <a href="/fotos/findAll/view" class="btn btn-secondary mt-3">← Volver a la lista</a>
  </div>

</body>
</html>
